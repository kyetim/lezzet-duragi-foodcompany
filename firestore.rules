rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Geçici olarak test modu - geliştirme aşamasında
    match /{document=**} {
      allow read, write: if true;
    }
    
    // TODO: Production için aşağıdaki kuralları kullanın:
    /*
    // Kullanıcılar kendi profil bilgilerine erişebilir
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Kullanıcılar kendi adreslerine erişebilir (userAddresses koleksiyonu)
    match /userAddresses/{addressId} {
      allow read, write: if request.auth != null && request.auth.uid == resource.data.userId;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.userId;
    }
    
    // Kullanıcılar kendi siparişlerine erişebilir (orders koleksiyonu)
    match /orders/{orderId} {
      allow read, write: if request.auth != null && request.auth.uid == resource.data.userId;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.userId;
    }
    
    // Menü öğeleri herkes tarafından okunabilir
    match /menu/{menuId} {
      allow read: if true;
      allow write: if false; // Sadece admin yazabilir
    }
    
    // Diğer tüm koleksiyonlar için varsayılan kural
    match /{document=**} {
      allow read, write: if false;
    }
    */
  }
}
